import styled from "styled-components";

export const StyledNoVideos = styled.h2`
    text-align: center;
    margin: auto;
    padding: 20px;
    color: ${({ theme }) => theme.color};
    background-color: transparent;
`;