//style
import { MiddleLineLetterStyle } from "./style";

export const MiddleLineLetter = ({ text }) => {
  return (
    <MiddleLineLetterStyle>
      <span>{text}</span>
    </MiddleLineLetterStyle>
  );
};
